<div class="row">
    <div class="col-auto">
        <h2>Welcome {{user.username}}</h2>
    </div>
</div>
<br>
{{#if posts.length}}
<div class="cards flex-column">
    {{#each posts as |post|}}
    {{> editdash post}}
    {{/each}}
</div>
{{/if}}